{
  "name": "storefront_v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph auth --product hosted-service $GRAPH_TOKEN && graph deploy --node https://api.thegraph.com/deploy/ myriadflow/storefront_v1",
    "create-local": "graph create --node http://localhost:8020/ myriadflow/storefront_v1",
    "remove-local": "graph remove --node http://localhost:8020/ myriadflow/storefront_v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 myriadflow/storefront_v1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.0",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
