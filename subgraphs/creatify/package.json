{
  "name": "creatifySubgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph auth --product hosted-service $GRAPH_TOKEN && graph deploy --node https://api.thegraph.com/deploy/ thisisommore/creatify-subgraph",
    "create-local": "graph create --node http://localhost:8020/ thisisommore/creatifySubgraph",
    "remove-local": "graph remove --node http://localhost:8020/ thisisommore/creatifySubgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 thisisommore/creatifySubgraph subgraph.local.yaml"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}